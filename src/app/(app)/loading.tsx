import { PageLoader } from "@/components/feedback/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      title="Loading workspace"
      description="Preparing your projects and assets…"
    />
  );
}

